// POST /api/auth/signup — register with employeeId, email, password, role
export async function signup(_req, res) {
  res.status(501).json({ message: 'Not implemented' });
}

// POST /api/auth/signin — login with email + password, returns token
export async function signin(_req, res) {
  res.status(501).json({ message: 'Not implemented' });
}
