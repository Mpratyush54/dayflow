// User model — fields per SRS:
//   employeeId, email, passwordHash, role ('EMPLOYEE' | 'HR' | 'ADMIN'),
//   personal details, job details, salary structure, documents, profilePicture
// TODO: define schema once a database is chosen (e.g. Mongoose / Prisma)
