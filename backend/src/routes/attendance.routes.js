import { Router } from 'express';

const router = Router();

// GET  /api/attendance            own daily/weekly attendance
// GET  /api/attendance/all        (admin) all employees' attendance
// POST /api/attendance/checkin
// POST /api/attendance/checkout

export default router;
